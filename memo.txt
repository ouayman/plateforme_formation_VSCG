Seed OK:
  admin@vsc.local (ADMIN)
   (PLANNER)
  coordinator@acme.local (COORDINATOR)
  trainer@vsc.local (TRAINER)
  participant@acme.local (participant parcours)


npm run docker:up    # Postgres + Mailpit
npm run db:push      # schéma
npm run db:seed      # admin@vsc.local
npm run dev