# Copier vers deploy/.env.deploy (gitignored) — clé SSH locale uniquement sur votre PC

VPS_HOST=212.132.109.214
VPS_USER=formation.valuestrea_96qgu6wiv1q
VPS_APP_DIR=/opt/vsc-platform
GIT_BRANCH=main

# Chemin vers clé privée locale (sans passphrase). Jamais dans le repo.
SSH_KEY_PATH=~/.ssh/vscg_deploy

# BDD locale (pull-prod-db.sh)
LOCAL_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vsc_platform?schema=public
PROD_DB_USER=vscg
PROD_DB_NAME=vsc_platform
